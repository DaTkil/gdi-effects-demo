This is a harmless fake malware that acts like a GDI virus. To open the file all you have to do is double-click the batch file. The virus will go on for a couple minutes unless you close the terminal window to stop it. Please note that this fake-virus is still beta and in is in the works!

## What it does
- Uses Windows GDI-based drawing effects
- Generates visual distortions and overlays
- Runs locally on the user's machine only
- Does NOT access files, network, or system settings
